# circle.yml
machine:
  services:
    - docker

dependencies:
  override:
    - docker pull krlmlr/kimisc-test:latest

test:
  override:
    - docker run -v $PWD:/checkout -w /checkout krlmlr/kimisc-test make test
