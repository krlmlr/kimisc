# circle.yml
machine:
  services:
    - docker

dependencies:
  override:
    - docker pull rocker/r-devel

test:
  override:
    - docker run rocker/r-devel pwd
    - docker run rocker/r-devel ls
    - docker run rocker/r-devel -v .:/checkout -w /checkout R CMD build .
