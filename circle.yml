# circle.yml
machine:
  services:
    - docker

dependencies:
  override:
    - docker pull rocker/r-devel

test:
  override:
    - docker run rocker/r-devel pwd
    - docker run rocker/r-devel ls
    - docker run -v .:/checkout -w /checkout rocker/r-devel R CMD build .
