# circle.yml
machine:
  services:
    - docker

dependencies:
  override:
    - docker pull rocker/hadleyverse:latest

test:
  override:
    - docker run -v $PWD:/checkout -w /checkout rocker/hadleyverse make install test
